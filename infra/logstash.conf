input {
  file {
    path => "/var/log/app/app.jsonl"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
  file {
    path => "/var/log/app/notifications.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    add_field => { "log_type" => "notifications" }
  }
}

filter {

  if [log_type] == "notifications" {
    grok {
      match => { "message" => "%{TIMESTAMP_ISO8601:timestamp} %{GREEDYDATA:notification}" }
    }
    date {
      match => ["timestamp", "ISO8601"]
      target => "@timestamp"
    }
  } else {
    json {
      source => "message"
    }

    # Если парсинг не удался — чтобы не спамило, просто дропаем
    if "_jsonparsefailure" in [tags] {
      drop { }
    }

    # Нормализуем timestamp
    if ![timestamp] and [ts] {
      mutate { rename => { "ts" => "timestamp" } }
    }

    if [timestamp] {
      date {
        match => ["timestamp", "ISO8601"]
        target => "@timestamp"
      }
    }

    # Опционально: убрать исходную строку, чтобы не дублировать
    mutate { remove_field => ["message"] }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
  }

  # Если хочешь тишину в консоли — закомментируй stdout
  # stdout { codec => rubydebug }
}
