version: "3.9"

services:
  mexc-short-dashboard:
    build: .
    container_name: mexc-short-dashboard
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/signals.db
    restart: unless-stopped
